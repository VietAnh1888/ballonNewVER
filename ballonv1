-- Inject bằng KRNL, Synapse X trong Pet Simulator 99

-- GUI khởi tạo
local CoreGui = game:GetService("CoreGui")
local gui = Instance.new("ScreenGui", CoreGui)
gui.Name = "FPS_BOOST_GUI"

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 250, 0, 300)
frame.Position = UDim2.new(0, 10, 0, 100)
frame.BackgroundColor3 = Color3.new(0.1, 0.1, 0.1)
frame.Active = true
frame.Draggable = true
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)

-- Tạo nút đơn giản
function createButton(text, posY, callback)
    local btn = Instance.new("TextButton", frame)
    btn.Size = UDim2.new(1, -20, 0, 30)
    btn.Position = UDim2.new(0, 10, 0, posY)
    btn.Text = text
    btn.TextColor3 = Color3.new(1,1,1)
    btn.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 18
    btn.MouseButton1Click:Connect(callback)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
end

-- Các nút tính năng
createButton("🧹 Anti Lag", 0, function()
    local lighting = game:GetService("Lighting")
    lighting.GlobalShadows = false
    lighting.FogEnd = 1e10
    lighting.Brightness = 0
    lighting.ClockTime = 12
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Decal") or v:IsA("Texture") then
            v:Destroy()
        elseif v:IsA("MeshPart") or v:IsA("Part") then
            v.Material = Enum.Material.SmoothPlastic
            v.Reflectance = 0
        end
    end
end)

createButton("🔇 Tắt Âm Thanh", 40, function()
    for _, s in pairs(game:GetDescendants()) do
        if s:IsA("Sound") then s.Volume = 0 end
    end
end)

createButton("🔢 Tắt Damage/Coins", 80, function()
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("BillboardGui") or v:IsA("TextLabel") then v:Destroy() end
    end
    workspace.DescendantAdded:Connect(function(obj)
        if obj:IsA("BillboardGui") or obj:IsA("TextLabel") then obj:Destroy() end
    end)
end)

createButton("🚀 FPS Cap = 30", 120, function()
    if setfpscap then setfpscap(30)
    else warn("Executor không hỗ trợ setfpscap()") end
end)

createButton("❌ Tắt UI phụ", 160, function()
    for _, ui in pairs(game.CoreGui:GetChildren()) do
        if ui:IsA("ScreenGui") and ui.Name ~= "RobloxGui" and ui.Name ~= "FPS_BOOST_GUI" then
            ui.Enabled = false
        end
    end
end)

-- BLACK SCREEN và NÚT ẨN ẢNH
createButton("🖤 Black Screen", 200, function()
    local black = Instance.new("Frame", CoreGui)
    black.Name = "BLACKOUT"
    black.Size = UDim2.new(1, 0, 1, 0)
    black.BackgroundColor3 = Color3.new(0, 0, 0)
    black.ZIndex = 9998

    -- Nút ảnh để tắt black screen
    local button = Instance.new("ImageButton", black)
    button.Size = UDim2.new(0, 300, 0, 170)
    button.Position = UDim2.new(0.5, -150, 0.5, -85)
    button.BackgroundTransparency = 1
    button.ZIndex = 9999
    button.Image = "rbxasset://textures/ui/GuiImagePlaceholder.png"

    -- Thay ảnh người dùng (tạm từ file local)
    -- Bạn phải dùng công cụ hỗ trợ như Synapse X (có custom asset ID) hoặc tải lên imgur rồi dùng link web-to-image converter nếu muốn ảnh thực

    button.MouseButton1Click:Connect(function()
        black:Destroy()
    end)
end)

createButton("❎ Đóng GUI", 240, function()
    gui:Destroy()
end)
